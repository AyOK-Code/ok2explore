# frozen_string_literal: true

RSpec.describe Ok2explore do
  it "has a version number" do
    expect(Ok2explore::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
